const elements =  {
    dbName: {
        table:{
            id: Integer,auto_increment,primary key;
            nome: NotNull,Varchar(100);
            cpf: notNull,char(15); 
        }
    }
    secondDbName:{
        table:{
            id: Integer,auto_increment,primary key;
            nome: NotNull,Varchar(100);
            cpf: notNull,char(15); 
        }
    }
    threeDbName:{
        table:{
            id: Integer,auto_increment,primary key;
            nome: NotNull,Varchar(100);
            cpf: notNull,char(15); 
        }
    }
}